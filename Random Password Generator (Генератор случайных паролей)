import tkinter as tk
from tkinter import ttk, messagebox
import json
import random
import string
import os
from datetime import datetime

class PasswordGeneratorApp:
    def __init__(self, root):
        self.root = root
        self.root.title("Random Password Generator")
        self.root.geometry("800x650")
        self.root.configure(bg='#f0f0f0')
        
        # История паролей
        self.history = []
        self.history_file = "password_history.json"
        
        # Загружаем историю при запуске
        self.load_history()
        
        # Создаем интерфейс
        self.create_widgets()
        
        # Центрируем окно
        self.center_window()
    
    def center_window(self):
        """Центрируем окно на экране"""
        self.root.update_idletasks()
        width = self.root.winfo_width()
        height = self.root.winfo_height()
        x = (self.root.winfo_screenwidth() // 2) - (width // 2)
        y = (self.root.winfo_screenheight() // 2) - (height // 2)
        self.root.geometry(f'{width}x{height}+{x}+{y}')
    
    def create_widgets(self):
        # Заголовок
        title_label = tk.Label(
            self.root, 
            text="Генератор случайных паролей", 
            font=("Arial", 20, "bold"),
            bg='#f0f0f0',
            fg='#2c3e50'
        )
        title_label.pack(pady=20)
        
        # Основной фрейм для настроек
        settings_frame = tk.LabelFrame(
            self.root, 
            text="Настройки пароля", 
           ("Arial", 12, "bold"),
            bg='#f0f0f0',
            fg='#34495e',
            padx=15,
            pady=15
        )
        settings_frame.pack(pady=10, padx=20, fill='x')
        
        # Длина пароля
        length_frame = tk.Frame(settings_frame, bg='#f0f0f0 length_frame.pack(pady=10, fill='x')
        
        length_label = tk.Label(
            length_frame, 
            text="Длина пароля:", 
            font=("Arial", 12),
            bg='#f0f0f0',
            fg='#2c3e50',
            width=15,
            anchor='w'
        )
       .pack(side='left')
        
        # Переменная для длины пароля
        self.length_var = tk.IntVar12)
        
        # Ползунок
        self.length_scale = tk.Scale(
            length_frame,
            from_=4,
            to=32,
            orient='horizontal',
            variable=self.length_var,
            bg='#f0f0f0',
            fg='#2c3e50',
            highlightthickness=0,
            length=300
        )
        self.length_scale.pack(side='left', padx=10)
        
        # Метка текущего значения
        self.length_value_label = tk.Label(
            length_frame,
            text="12",
            font=("Arial", 12, "bold bg='#f0f0f0',
            fg='#e74c3c',
            width=3
        )
        self.length_value_label.pack(side='left')
        
        # Обновляем метку при изменении ползунка
        self.length_var.trace('w', self.update_length_label)
        
        # Чекбоксы для выбора символов
        checkboxes_frame = tk.Frame(settings_frame, bg='#f0f0f0')
        checkboxes_frame.pack(pady=15, fill='x')
        
        # Чекбоксы
        self.use_upper = tk.BooleanVar(value=True)
        self.use_lower = tk.BooleanVar(value=True)
        self.use_digits = tk.BooleanVar(value=True)
        self.use_special = tk.BooleanVar(value=False)
        
        upper_check = tk.Checkbutton(
            checkboxes_frame,
            text="Заглавные буквы (A-Z)",
            variable=self.use_upper,
            font=("Arial", 11),
            bg='#f0f0f0 fg='#2c3e50',
            anchor='w'
        )
        upper_check.pack(pady=5, fill='x')
        
        lower_check = tk.Checkbutton(
            checkboxes_frame,
            text="Строчные буквы (a-z)",
            variable=self.use_lower font=("Arial", 11),
            bg='#f0f0f0',
            fg='#2c3e50',
            anchor='w'
        )
        lower_check.pack(pady=5, fill='x')
        
        digits_check
